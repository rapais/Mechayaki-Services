export default function TabletMenuPage() {
  return (
    <div style={{ display: "grid", gap: 10 }}>
      <h1>Tablet Menu + Cart</h1>
      <p>Placeholder. We will wire menu/items + cart + create order.</p>
    </div>
  );
}
